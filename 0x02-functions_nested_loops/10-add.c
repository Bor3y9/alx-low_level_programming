#include "main.h"

/**
 *add - function that adds two integers
 *@x: number to be targeted
 *@y: number to be targeted
 *Return: returns the sum of n and m
 */
int add(int x, int y)
{
int sum = x + y;
return (sum);
}
