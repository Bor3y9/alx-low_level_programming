#include<stdlib.h>
#include<time.h>
#include<stdio.h>
/**
 * main - checking for negativity
 *
 * Return:0(sucess)
 */
int main(void)
{

	int ch;
	for(ch = 48; ch <= 57; ch++)
	{
		putchar(ch);
	}
	putchar(10);

	return(0);
}
